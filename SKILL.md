---
name: delta-harmonica-score
description: 把歌曲简谱转成《三角洲行动》守夜人口琴的键盘琴谱并渲染成 PNG 图片（升调红底、降调绿底、歌词逐字对齐、长按/半长按框内标注）。当用户上传或粘贴简谱、要求做三角洲口琴谱、琴谱对照表、口琴简化谱、把五线谱/简谱转成 ZXCVBNM 键位图时使用。
agent_created: true
---

# 三角洲行动 · 口琴谱生成

## 用途

把一首歌的简谱（图片、文本或用户口述）转写成《三角洲行动》「守夜人口琴」的键盘琴谱，
并渲染成一张干净的白底 PNG：键盘字母装在方框里、歌词逐字对齐在方框下方，
**升调红底、降调绿底**，长按与半长按在框内标出。

产物是给人看着弹的图，所以优先级永远是：**清楚 > 好看 > 信息量**。

## 一、核心映射（硬规则，不许自行改动）

**音级 → 键盘**（键盘上连续的一排，正好是 C 大调音阶）：

| 简谱 | 1 | 2 | 3 | 4 | 5 | 6 | 7 | 1̇ |
|---|---|---|---|---|---|---|---|---|
| 按键 | Z | X | C | V | B | N | M | , |

**八度 → 修饰键 + 底色**：

| 简谱记号 | 游戏内操作 | 图片底色 |
|---|---|---|
| 中音（数字无点） | 直接按键 | 白底黑字 |
| 高音（数字上方一点） | 按住鼠标**右**键（+12 半音）后再按该键 | **红底** |
| 低音（数字下方一点） | 按住鼠标**左**键（−12 半音）后再按该键 | **绿底** |
| 升/降号 `#` `b` | 按住鼠标**中**键（+1 半音） | 黄底（仅在用到时出现图例） |

**时长 → 框内符号**：

| 原谱 | 图片表现 | DSL 写法 |
|---|---|---|
| 长音 / 长按 | 字母右侧一条短横 | `3-` |
| 附点 / 稍短的长音 | 字母右侧一个圆点 | `3·` 或 `3.` |

> 八度与修饰键的对应关系来自游戏内实测复现数据：8 个音级键的半音偏移为
> 0/2/4/5/7/9/11/12，左键整体 −12、右键整体 +12、中键整体 +1，左右互斥且中键可叠加。
> 因此高音 1 也可以只按 `,`，但**本 skill 统一用「右键 + Z」表示**，和红底标注保持一致。

## 二、工作流

1. **取谱**：用户给了简谱图片就直接读图识谱；给了文本就直接解析；只给了歌名/音频则说明需要简谱，不要凭记忆编谱。
2. **转写**：逐句转成 DSL（格式见下），写到工作区的 `<歌名>.txt`。
3. **校验**：按第六节清单逐条核对，尤其是「音符数 = 歌词数」和八度点。
4. **渲染**：调用脚本生成 PNG。
5. **交付**：把 PNG 放入工作区，用 present_files 展示；再顺带在回复里贴一份纯文本数字简谱对照（手机上可以直接照抄）。
6. 用户若指定「某句不对」「第二段漏了」，只改 DSL 对应行后重跑，不要重写整份谱。

## 三、DSL 格式

一个乐句一行，音符和歌词用 `|` 分开；歌词与音符**严格 1:1**，没有歌词的音符写 `-`：

```
# 歌名
// 以 # 开头是曲名（不含书名号），// 是注释，空行忽略
// 音符记号： ^1 高音（红底）  _6 低音（绿底）  #4 升半音  b7 降半音  3- 长按  1· 半长按  0 休止
// 音符行里的 / 表示换气间隔，只影响排版；歌词行里写不写 / 都会被忽略

3 2 4 3 1 5 7 ^1 7 5 1 | 刮 风 这 天 我 试 过 握 着 你 手
1 6 6 / 6 5 5 5 4 3 2 3 4 3- | 但 偏 偏 雨 渐 渐 大 到 我 看 你 不 见
```

要点：

- 音符记号的正则：`([#b]?)(^+|_{1,2})?[0-7]([#b]?)([-.·]?)`，即 `^` 高八度、`_` 低八度、`#`/`b` 半音（写在数字前或后都认）、`-` 长按、`·`/`.` 半长按。
- 英文歌词一个词一个槽位即可（`oh bye bye` 就是 3 个槽位）。
- 一个音符对应两个汉字时（少见），把两个字写在同一槽位里（不空格）。
- 长乐句会自动折行，不用手动拆行；但**换段、换主歌副歌时建议另起一行**，版面更清楚。

## 四、渲染脚本

```bash
python <skill_dir>/scripts/render_score.py <输入.txt> -o <输出.png> --scale 2
```

- 依赖：Pillow（Windows 上优先用 `C:\Users\<用户>\.workbuddy\binaries\python\versions\<ver>\python.exe`，该环境已装 PIL）。
- `--scale` 默认 2，输出约 2300px 宽，手机看很清楚；要更小文件用 1，要更清晰用 3。
- `--title 晴天` 可覆盖曲名。
- 字体自动在 Windows/macOS/Linux 的常见中文字体里找（宋体做字母和歌词、雅黑做标题），找不到会降级为默认字体，不要因此报错中断。
- 脚本会把「音符/歌词数量不一致」等问题打到 stderr，**必须处理完再交付**。

## 五、输出规格

图片自动包含：

- 居中标题《 歌 名 》和一条浅灰分隔线；
- 右上角图例：降调（绿）、升调（红）、半长按（点）、长按（短横），若谱中出现半音会多一行「半音（黄）」；
- 左手边逐行排列的方框琴谱，字母居中、时长符号在框内；
- 方框正下方逐字对齐的歌词，无歌词位置留空。

## 六、交付前自检清单

- [ ] 每个乐句的音符数 = 歌词数（缺歌词处用 `-` 占位）。
- [ ] 简谱里的高低八度点**一个都没漏**：漏一个红/绿底就错一个音。
- [ ] 长音、延音线、附点都转成了 `-` 或 `·`，没有当成普通音符。
- [ ] 反复记号（D.C./反复号）已展开成线性顺序，或明确告知用户只做了第一遍。
- [ ] 图里没有多余空行、没有孤零零一两个音符的碎行。
- [ ] 曲名与歌词没有错别字（歌词按用户给的原样，不要自行润色）。

## 七、边界

- 只处理**单声部旋律**；和声、多声部、伴奏请让用户指明取哪一声部。
- 音域：中音/高音/低音各一组是稳妥范围；倍高音仅 `^^1`（即右键 + `,`）可达，其余倍高音、倍低音超出乐器音域，遇到要提示用户改用其他八度。
- 半音（中键）在实战里是「按住中键再按键」，操作别扭，谱子里偶尔出现没问题，整首歌大面积半音要提醒用户。
- 快歌里的十六分音符对普通人是弹不出来的，遇到密集段落建议只保留主旋律骨干音，并在回复里说明做了简化。

详细映射与音域说明见 `references/mapping.md`；
读简谱图片时容易被忽略的记号见 `references/jianpu.md`；
可跑通的完整样例见 `examples/晴天.txt` 与 `examples/demo-晴天.png`。
